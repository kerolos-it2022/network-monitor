# ملفات لا تُنسخ داخل صورة Docker (لتقليل الحجم وتسريع البناء)
node_modules
**/node_modules

# ملفات البيئة الحساسة (تُمرّر عبر docker-compose environment بدلاً من نسخها)
backend/.env
.env

# ملف قاعدة البيانات الفعلي (يُنشأ داخل volume عند أول تشغيل)
database/monitoring.db

# سجلات وملفات مؤقتة
*.log
npm-debug.log*
.git
.gitignore
.dockerignore

# ملفات نظام التشغيل
.DS_Store
Thumbs.db

# ملفات IDE
.vscode
.idea

# السكريبتات المؤقتة التي استخدمناها أثناء التطوير فقط
backend/_init-db.js
**/PROGRESS.md
